# Task 06: Frontend — Paper Library

Build the paper library sidebar and upload UI.

## API Client

- [ ] Create `frontend/src/lib/api.ts` — typed fetch wrapper for paper endpoints
- [ ] Write tests for the API client (mock fetch)

## Components

- [ ] Paper list component — displays papers with title, date, size
- [ ] Upload component — drag-and-drop zone + file picker, `.pdf` only
- [ ] Delete button with confirmation
- [ ] Wire up to the main layout (left sidebar)

## State

- [ ] Svelte 5 runes for paper list state (`$state`, `$derived`)
- [ ] Selected paper state (drives the PDF viewer)
